using UnityEngine;
using System.Collections;

public class cubeRotation : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//transform.Rotate(Random.Range(0f,5f),Random.Range(0f,5f),Random.Range(0f,5f));
	
	}
}
